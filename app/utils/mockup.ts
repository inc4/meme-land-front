import { sleep } from "./other";

export const checkIsVerified = async () => {
  await sleep(1500);

  const verified = true;

  return verified;
};

export const checkInviteCode = async () => {
  await sleep(1500);

  const valid = true;

  return valid;
};